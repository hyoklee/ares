Test branch `886-blob-replicas`.
Run ior at 8 and 12 nodes scale over `clio-fs` and measure the performance.
The `clio_default.yaml` should include the full stack of cache + replication,
but the config should do something.

